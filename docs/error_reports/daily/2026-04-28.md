# Daily Stability Report (2026-04-28)

## Context

- Base dir: `.`
- Project: `demo-project`
- Mode: `advisory`
- Timezone: `Asia/Shanghai (CST)`

## Commands Run

### 1) `git diff --check`

- Command: `git diff --check`
- Result: **pass**
- Output: (none)

### 2) Regression (strict)

- Command: `python3 pm-prd-copilot/scripts/run_regression.py --base-dir . --strict`
- Result: **pass**
- Output:
  - `Regression checks passed.`

### 3) Harness audit + efficiency

- Command: `python3 harness/run_harness.py --base-dir . --project demo-project --mode advisory --audit --efficiency`
- Result: **pass** after the方案二 governed rerun
- Summary:
  - `Harness status: pass`
  - All checkers: pass
- Evidence:
  - Report: `projects/demo-project/runs/pipeline-latest/harness_report.json`

## Overall Status

- `git diff --check`: pass
- regression (`--strict`): pass
- harness (`--audit --efficiency`): pass after方案二 governed rerun
- Independent GPT-5.5 review initially kept the report at **warn** because `BUG-2026-04-28-001` was still open at that time; after the approved fix and方案二 rerun, the old non-governed `pipeline-latest` warning is cleared.

## Fix Implemented After Approval

- Approval: user approved方案二加方案四.
- Pipeline fix: `run_pipeline.py` now supports `--governed` and blocks approval-gated stages when required approvals or assumption overrides are missing.
- Trace fix: pipeline manifests now record `governance_mode`, `approval_gate_enforced`, and `required_approvals`.
- Harness fix: `workflow_gate_checker.py` now warns when a production pipeline run reaches approval-gated stages without enforced approval gates or without required approval declarations.
- Regression fix: `run_regression.py --strict` now verifies that `--governed` blocks a PRD run with missing approvals.
- Initial post-fix verification before方案二 rerun:
  - `python3 -m py_compile ...`: pass
  - `git diff --check`: pass
  - `python3 pm-prd-copilot/scripts/run_regression.py --base-dir . --strict`: pass
  - `python3 harness/run_harness.py --base-dir . --project demo-project --mode advisory --audit --efficiency`: warn as expected

##方案二 Governed Rerun

- Time: `2026-04-28 09:48 CST`
- Action: recorded temporary pipeline-specific test assumption overrides for `demo-project`, then reran `pipeline-latest` with `--governed`.
- Command: `python3 pm-prd-copilot/scripts/run_pipeline.py --base-dir . --project demo-project --stage all --mode rule --governed`
- Result: pass
- Command: `python3 harness/run_harness.py --base-dir . --project demo-project --mode advisory --audit --efficiency`
- Result: pass
- Evidence: `projects/demo-project/runs/pipeline-latest/manifest.json` now records `governance_mode: governed` and `approval_gate_enforced: true`.

## Interaction Review

- Time: `2026-04-28 09:53 CST`
- Finding: using global `assumption_overrides.prototype_preview` for the demo rerun could affect `prototype_preview_gate_checker.py`, because that checker also treats the same global override as prototype approval.
- Fix: moved demo-only coverage to `pipeline_assumption_overrides` and updated `run_pipeline.py --governed` to honor that pipeline-specific field.
- Result: `demo-project` keeps harness pass without granting broader workflow or prototype approval.
- Follow-up fix: centralized pipeline stage-to-workflow action mapping through `governance_trace.py` and production manifest `stage_actions`; `workflow_gate_checker.py` now reads manifest/trace instead of keeping a second hard-coded mapping.
- Concurrency fix: JSON writes in pipeline/harness shared helpers now use temp-file plus atomic replace to avoid concurrent harness runs reading partially written files.
- Result: no remaining mapping-duplication watch item.

## Independent GPT-5.5 Review

- Review time: `2026-04-28 08:39 CST`
- Note: this review happened before the later approved方案二加方案四 fix.
- Commands rerun:
  - `git diff --check`: pass
  - `python3 pm-prd-copilot/scripts/run_regression.py --base-dir . --strict`: pass
  - `python3 harness/run_harness.py --base-dir . --project demo-project --mode advisory --audit --efficiency`: pass
- New bugs: none
- Repeated bugs: `BUG-2026-04-28-001`
- Fixed bugs: none
- Suspected cause: fast draft pipeline and governed workflow remain separate contracts; harness pass does not close that governance gap.
- Next action: user decision required before adding a governed pipeline mode, pipeline preflight gate, or checker change.
- User approval needed: no for audit-only recording; yes for any stable governance, harness, pipeline, or publishing behavior change.

## New Bugs

- None found in this run.

## Repeated Bugs / Watch Items

- None after方案二 governed rerun.

## Fixed Bugs

- `BUG-2026-04-28-001` fixed after user-approved方案二加方案四.

## Unresolved Bugs

- None currently recorded as `open`, `blocked`, or unfixed in `docs/error_reports/bug_log.md`.
- No remaining watch item for `pipeline-latest`; harness is pass.

## Suspected Cause

- Previous fast draft pipeline did not enforce `prd_workflow.yaml` approvals and harness did not inspect pipeline governance declarations.

## Next Action

- Use `--governed` for approval-gated production pipeline runs.
- Do not treat the demo-project pipeline-specific assumption overrides as real product approvals; they are only test coverage for clearing the demo run and should not imply prototype approval.

## User Approval Needed?

- No additional approval needed for the implemented fix; user already approved方案二加方案四.
- Approval is still needed before adding future stable governance, harness, pipeline, or publishing behavior changes beyond this fix.
