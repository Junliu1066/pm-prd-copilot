# 2026-04-30 错误与治理健康报告

## 运行前检查

| 项目 | 结果 |
|---|---|
| cwd | `/Users/liujun/Desktop/产品经理skill` |
| branch | `main` |
| HEAD | `22ad7ff` |
| dirty file count | `78` |
| referenced template exists | Yes: `docs/daily_report_template.md`; daily report guide also exists at `docs/error_reports/daily/README.md` |
| will write files | Yes |
| write paths | `docs/error_reports/daily/2026-04-30.md`; `docs/error_reports/bug_log.md` |

## 检查结果

| 命令 | 结果 | 说明 |
|---|---|---|
| `git diff --check` | pass | 本轮错误报告收口前，当前工作区无 whitespace 错误。 |
| `python3 pm-prd-copilot/scripts/run_regression.py --base-dir . --strict` | pass | 输出：`Regression checks passed.` |
| `python3 harness/run_harness.py --base-dir . --project demo-project --mode advisory --check-only --audit --efficiency` | pass | Harness status pass；所有列出的检查均通过；未写项目文件。 |

## 今日已修复或确认的重点

| 事项 | 状态 | 处理效果 |
|---|---|---|
| PRD 主链路 | fixed | PRD 输出口径已稳定为页面说明、页面跳转关系、原型图层；非 AI 项目不强行输出 AI 模型选型。 |
| pipeline governance | fixed | pipeline 默认 governed；快草稿必须显式 `--fast-draft`。 |
| harness check-only | fixed | 日常检查默认不写项目文件；需要报告刷新时必须显式 `--write-report`。 |
| preference cache | fixed | `clear` / `archive-clear` 必须显式 `--approved-by-user`；cleared 状态必须记录用户批准。 |
| conditional harness checkers | fixed | `delivery_plan` 和 `ai_solution` checker 已提交；未请求时 pass，不污染普通 PRD。 |
| UI / UX 风格选择器 | candidate fixed | 已补齐 router 引用依赖；只作为后期 UI / UX 候选能力，不进入 PRD 默认主链路。 |
| internal delivery package | candidate fixed | 已泛化内部完整包脚本；可生成可信团队内部包，默认排除 runs、memory-cache、已有 zip。 |
| root/archive cleanup | review only | 已形成中文审查表；未接受 root 删除状态，未提交 archive。 |

## Bug 状态

| ID | 状态 | 当前结论 |
|---|---|---|
| `BUG-2026-04-28-001` | fixed | 生产 pipeline 绕过 governed workflow 的问题仍保持修复状态。 |
| `BUG-2026-04-30-001` | fixed | 候选/按需能力曾存在“已被入口引用但文件未提交”的风险；D2/D3 已补齐。 |
| `BUG-2026-04-30-002` | fixed | 项目偏好缓存清空缺少命令级显式批准门槛；已补 `--approved-by-user` 和 regression。 |
| `BUG-2026-04-30-003` | fixed | 内部完整包脚本旧项目硬编码导致 demo / fitness 不可用；已泛化并通过真实输出复核。 |

## 未解决但已隔离的风险

| 风险 | 当前处理 | 下一步建议 |
|---|---|---|
| root 删除项 | 已列入 E 批中文审查表 | 等用户逐条批准是否接受 root 删除状态。 |
| `docs/archive/` | 只作为本地归档证据 | 先定归档策略，再决定是否提交。 |
| `memory-cache/` | 只作为项目内偏好缓存 | 等 `fitness-app-mvp` closeout / 归档前处置。 |
| `projects/*` | 归为 C 批项目产物 | 后续逐项目 closeout，不混入治理提交。 |
| `ai-intel/raw/` | 只作为本地复核证据 | 后续决定归档候选或 30 天后删除候选。 |

## 解决建议

推荐下一步：

1. 先提交本 B4 错误报告证据。
2. 再筛选 `docs/proposals/*`：保留长期追溯价值，临时 staging / commit 材料暂缓。
3. 再处理 C 批项目 closeout，优先 `demo-project` 和 `fitness-app-mvp`。
4. 最后逐条拍板 E 批 root/archive 删除状态。

不建议今天新增新的 stable skill、harness、workflow、plugin 或长期规则。

## 需要用户批准的事项

- 是否提交本错误报告证据。
- 是否接受 root 删除项。
- 是否提交 archive 目录。
- 是否提交或清空 `memory-cache/`。
- 是否把项目经验写入长期记忆。
- 是否归档或删除任何项目产物。
