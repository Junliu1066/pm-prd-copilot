# 长期偏好逐条审查清单

- 日期：2026-04-30
- 状态：审查清单，不批准 staging / commit / 长期记忆写入
- 范围：`pm-prd-copilot/memory/user_preferences.md`
- 原则：长期记忆必须逐条批准；项目偏好不能自动跨项目复用。

## 结论

当前长期偏好文件里大部分内容来自你已经明确表达过的治理偏好，但仍不建议直接提交。原因是：有些条目范围较宽，容易把项目经验或阶段性规则固化成永久规则。

建议先按本清单逐条确认，再提交长期偏好文件。

## 分类规则

| 分类 | 含义 | 后续动作 |
|---|---|---|
| `approved_long_term` | 已明确批准，可进入长期偏好 | 可以保留，提交前再做措辞检查。 |
| `needs_rewording` | 方向正确，但范围太宽或表述需收敛 | 先改措辞，再提交。 |
| `needs_user_approval` | 可能有价值，但还需要你单独确认 | 不确认不写入长期记忆。 |
| `project_only` | 只适用于某个项目 | 放项目缓存或 closeout，不写全局长期偏好。 |
| `do_not_keep` | 不应保留为长期偏好 | 删除或不提交。 |

## 长期偏好逐条审查

| 条目 | 当前分类 | 我的建议 | 原因 |
|---|---|---|---|
| 默认输出中文 | `approved_long_term` | 保留 | 你已明确要求报告给中文。 |
| 直接、结构化、实用的决策风格 | `approved_long_term` | 保留 | 符合长期协作方式。 |
| 长期稳定可靠优先；一行代码能解决就不写多行 | `approved_long_term` | 保留 | 你已明确要求写入长期记忆。 |
| 方案 / PR 需要明确人工批准 | `approved_long_term` | 保留 | 和当前监督机制一致。 |
| 长期规则写入前必须先给方案 | `approved_long_term` | 保留 | 已在治理规则中反复确认。 |
| PRD delivery standard 固定包含多个视觉图 | `needs_rewording` | 收敛 | 当前表述像“所有 PRD 固定五图”，建议改成“页面说明、页面跳转关系、原型图层为底线，其他图表按项目必要性放对应章节”。 |
| PRD 原型图层规则 | `approved_long_term` | 保留 | 你已确认 PRD 需要页面级原型说明，但 PNG/HTML/UI 需后续确认。 |
| 原型交付链路 | `approved_long_term` | 保留并精简 | 方向正确，可稍微压缩，避免过长。 |
| 项目偏好缓存规则 | `approved_long_term` | 保留 | 项目内使用、归档前对齐、长期记忆需批准，边界清楚。 |
| “开发文档”默认 Codex 开发文档 | `approved_long_term` | 保留 | 你明确说过以后默认如此。 |
| 产品文档和开发文档边界 | `approved_long_term` | 保留 | 防止 PRD 混入内部治理机制。 |
| Codex 开发框架规则 | `needs_rewording` | 收敛 | 方向正确，但当前列举过长，建议压成“内部版 Codex 开发文档需包含任务包、边界、验证、人工门禁、复盘”。 |
| Framework protection rule | `approved_long_term` | 保留 | 内部版 / 外部保护版边界已反复确认。 |
| 一期 / 二期 / 三期 / 最终 | `approved_long_term` | 保留 | 你已确认开发文档分期口径。 |
| B 包英文规则 | `approved_long_term` | 保留 | B 包外部分发要求明确。 |
| “我自己开发”不等于个人产品 | `approved_long_term` | 保留 | 可避免误判产品受众。 |
| framework exposure rule | `needs_user_approval` | 暂不提交 | “不需要藏着”是条件性表达，建议保留为条件规则，不默认暴露。 |
| 文档受众分层 | `approved_long_term` | 保留 | PRD / 开发文档 / Codex 内部文档分层清楚。 |
| Codex 内部文档可执行性 | `approved_long_term` | 保留 | owner、输入、输出、允许/禁止路径、验证、人工确认是必要底线。 |
| 开发包默认包含所有资产 | `needs_rewording` | 收敛 | “所有资产”过宽，建议改成“按受众、阶段、脱敏策略包含执行所需资产”。 |
| “先别改动”只分析不编辑 | `approved_long_term` | 保留 | 这是明确安全边界。 |

## 项目偏好不进入长期记忆

`fitness-app-mvp` 里的以下内容只能保持项目内：

| 项目偏好 | 分类 | 原因 |
|---|---|---|
| 中国市场默认分析 | `project_only` | 只适用于该健身项目，不能默认套到所有项目。 |
| 深色机械风原型方向 | `project_only` | 是该项目 UI 风格，不是通用默认风格。 |
| 训练计划优先执行路径 | `project_only` | 是健身产品业务逻辑。 |
| “改计划”替代“改重量” | `project_only` | 是健身产品交互细节。 |
| 手动开始下一组 | `project_only` | 是健身产品节奏设计。 |

## 建议修改方向

如果你后续批准修改长期偏好文件，建议只做措辞收敛，不扩展新规则：

1. 保留已明确批准的长期偏好。
2. 把 PRD 视觉层从“固定五图”改成“底线 + 按项目必要性选择”。
3. 把 Codex 开发框架规则压缩成底线，不列过多内部机制。
4. 把开发包“所有资产”改成“按受众、阶段、脱敏策略包含执行所需资产”。
5. framework exposure 改成条件规则：只有用户明确要求框架产品化或暴露时才适用。

## 需要你后续拍板

| 决策 | 我的建议 |
|---|---|
| 是否按本清单修改长期偏好文件 | 建议是 |
| 是否提交 `pm-prd-copilot/memory/user_preferences.md` | 修改后再单独提交 |
| 是否提交 `memory-cache/` | 暂不提交 |
| 是否把健身项目偏好提炼为长期规则 | 暂不，保持项目内 |
| 是否清空项目偏好缓存 | 暂不，等项目 closeout / 归档前对齐 |

## 本轮未执行

- 未修改长期偏好文件。
- 未提交长期偏好。
- 未提交项目缓存。
- 未清空缓存。
- 未把项目偏好写入长期记忆。
