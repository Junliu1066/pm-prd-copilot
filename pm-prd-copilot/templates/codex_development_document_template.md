# Codex 开发文档模板

- 文档目标：把 PRD 转成可交给 Codex 执行的开发文档。
- 默认模式：轻量 Codex 开发文档。
- 可选完整模式：仅在用户明确要求 full internal / agentic delivery / 多阶段开发治理 / Skill-MCP-Harness 改造时启用。
- 配套 PRD：
- 文档状态：
- 分发等级：内部执行版。外部分发必须改用 `external_protected_development_document_template.md`。
- 最后更新：

---

## 0. 使用模式与边界

### 0.1 默认轻量模式

普通开发就绪 PRD、单阶段开发计划，或用户只说“开发文档”时，默认使用轻量模式。

轻量模式只写清：

- 文档目标。
- 输入材料。
- 文档状态：`Draft / 方向对齐版`、`Ready / 开发执行版` 或 `Execution Plan / 执行拆解版`。
- 开发范围。
- Ready Gate Checklist。
- 任务包。
- 允许修改范围。
- 禁止修改范围。
- 人工确认点。
- 验证命令。
- 回滚方案。
- 待决策项。
- 审核结论。

轻量模式默认不输出：

- phase 1 / phase 2 / phase 3 / final 全套 Codex 计划。
- 完整 Skill / MCP / Harness 操作系统说明。
- 内部多管家 / 部门体系全量展开。
- development governance report。
- 长期记忆、skill、harness、registry 的稳定规则变更。

### 0.2 完整内部模式

只有出现以下情况，才启用本文档末尾的“完整内部治理附录”：

- 用户明确要求半自动 agentic delivery。
- 用户明确要求一期 / 二期 / 三期 / 最终阶段 Codex 计划。
- 项目已批准能力启用、MCP 接入、harness 改造、registry 变更或多 agent 治理范围。
- 当前任务本身就是治理架构、skill、harness、plugin、workflow 或长期规则修复。

完整内部模式可以引用附录，但仍必须遵守：

- 如无必要，不新增 skill / harness。
- 支线任务不能影响主线任务。
- 长期记忆、stable 转正、删除、归档、push / PR 必须人工批准。

### 0.2.1 多分支执行机制启用条件

当开发文档涉及多模块、多页面、多接口、多端联调、AI 能力、数据库/API 契约、UI/原型并行、agentic delivery，或用户明确要求分支并行时，启用 `Codex 多分支工程执行操作系统`。

单文件小修、低风险文档修正、单模块小改默认不启用完整多分支机制，只记录“无需多分支”的判断和原因。

### 0.3 外部分发边界

如果本文档要发给外部开发者、外包团队、合作方或投资人，必须改用 `external_protected_development_document_template.md`，不得直接分发内部执行版。

---

## 0.4 文档状态与 Ready Gate

### 0.4.1 文档状态

| 状态 | 用途 | 是否可进入开发 |
|---|---|---|
| `Draft / 方向对齐版` | 对齐方向、范围、模块和关键问题 | 否 |
| `Ready / 开发执行版` | 工程契约已完整，可进入实现 | 是 |
| `Execution Plan / 执行拆解版` | T0-Tn 任务包，可交给实现方执行 | 是 |

状态判断：

- 没有工程契约 = 不能开发。
- 只有方向和模块 = 对齐文档。
- 有 DB/API/状态/测试/回滚/任务包 = 开发执行文档。

### 0.4.2 Ready Gate Checklist

| Gate | 是否适用 | 通过标准 | 当前结论 |
|---|---|---|---|
| Development Ready Gate | 是 | P0/P1 决策已确认；DB、API、前端状态、Worker、测试、部署、回滚明确；T0-Tn 可独立开发、验证、回滚 |  |
| AI Evaluation Gate | 项目涉及 AI 时适用 | 模型角色、输入输出 schema、rubric、阈值、failure_type、追溯字段和异常处理明确 |  |
| Async Reliability Gate | 项目涉及异步任务时适用 | 状态机、幂等、重试、取消、snapshot、历史不可覆盖规则明确 |  |
| Candidate Publish Gate | 项目涉及候选转正式时适用 | 回归/验证结果、人工决策、发布记录、退化保护、配置一致性、禁止自动发布明确 |  |

未通过处理：

- Development Ready Gate 未通过：文档只能标记为 `Draft / 方向对齐版`。
- AI Evaluation Gate 未通过：AI 功能不能标记可验收。
- Async Reliability Gate 未通过：异步任务不能进入开发。
- Candidate Publish Gate 未通过：候选结果不能发布为正式结果。

### 0.4.3 AI Evaluation Gate 模板

仅项目涉及 AI 能力时填写。

| 项 | 内容 |
|---|---|
| 模型角色 | answer / teacher / optimizer / embedding 等 |
| 输入 schema |  |
| 输出 schema |  |
| 结构化输出失败处理 |  |
| rubric / 评分口径 |  |
| pass 阈值 |  |
| confidence 阈值 |  |
| failure_type |  |
| 追溯字段 | model profile、prompt/template version、scoring_config、metadata |
| 异常处理 | 低置信度、高风险、格式错误、模型超时、模型不可用 |

### 0.4.4 Async Task Contract 模板

仅项目涉及异步任务时填写。

| 项 | 内容 |
|---|---|
| 任务类型 | Run / Evaluation / Optimization / Export / other |
| 状态机 | queued / running / succeeded / failed / canceled / needs_review |
| 幂等规则 | Idempotency-Key 或业务唯一键 |
| 重试规则 | 最大次数、可重试错误、不可重试错误 |
| 取消规则 | 可取消状态、不可取消状态 |
| snapshot 固化 | 输入、配置、模型、版本 |
| 历史保留 | 历史任务、评分、日志不能覆盖，只能新增或状态变更 |

### 0.4.5 Candidate Publish Gate 模板

仅项目涉及候选结果转正式结果时填写。

| 项 | 内容 |
|---|---|
| 候选来源 |  |
| 回归 / 验证结果 |  |
| 人工决策记录 | 意见必填 |
| 发布记录 | 来源候选、对比结果、评审意见、发布人、发布时间 |
| key case 退化处理 | 默认不建议发布 |
| high risk case 退化处理 | 默认不建议发布 |
| 评测配置一致性 | 评分配置、Dataset、teacher model 等不一致时不能给发布建议 |
| 自动发布限制 | 候选结果不能自动发布，必须走门禁 |

---

## 1. 文档目标

- 本开发文档要完成什么：
- 交给 Codex 后应达到的效果：
- 本轮不解决什么：

---

## 2. 输入材料

### 2.1 已确认事实

-

### 2.2 当前假设

| 假设 | 影响范围 | 验证方式 | 失效时处理 |
|---|---|---|---|
|  |  |  |  |

### 2.3 待决策问题

| 优先级 | 问题 | 影响 | 当前处理 |
|---|---|---|---|
| P0 |  | 阻塞开发或影响方向 | 待确认 |
| P1 |  | 可带假设推进 | 标注假设 |
| P2 |  | 不阻塞首版 | 后续确认 |

### 2.4 必备产品输入

- PRD：
- 页面说明：
- 页面跳转关系：
- PRD 原型图层：
- 核心流程 / 状态流转：
- 用户故事与验收标准：
- In Scope：
- Out of Scope：
- AI 模型选型：仅涉及 AI 能力时填写；非 AI 项目不补 AI 选型。

---

## 3. 开发范围

### 3.1 本轮开发范围

| 模块 / 页面 / 能力 | 目标 | 主要变更 | 验收标准 |
|---|---|---|---|
|  |  |  |  |

### 3.2 本轮不做

-

### 3.3 影响面

| 类型 | 是否涉及 | 说明 |
|---|---|---|
| 前端页面 / 交互 |  |  |
| 后端接口 / 服务 |  |  |
| 数据库 / 数据模型 |  |  |
| 权限 / 账号 |  |  |
| AI / Prompt / 模型 |  |  |
| 第三方 API / MCP |  |  |
| 发布 / 部署 |  |  |

---

## 4. 任务包

每个任务必须能独立执行、验证和回滚。

| 任务 ID | 目标 | 输入材料 | 允许修改范围 | 禁止修改范围 | 预期输出 | 验证命令 | 人工确认点 |
|---|---|---|---|---|---|---|---|
| T1 |  |  |  |  |  |  |  |

### 4.1 最小修复策略

- 优先复用现有代码、模板、脚本和检查。
- 只做能解决当前问题的最小改动。
- 不把项目特例提炼成长期规则。
- 不在没有批准的情况下新增 skill、harness、workflow、plugin 或 automation。

### 4.2 Codex 多分支工程执行操作系统

机制目标：把 Codex 开发文档转化为可并行、可审核、可回滚、可治理的工程执行计划。

启用判断：

| 判断项 | 结论 | 证据 |
|---|---|---|
| 是否需要多分支 |  |  |
| 是否可降级为单分支 |  |  |
| 是否存在 blocked 分支 |  |  |
| 是否需要完整内部模式 |  |  |

核心链路：

```text
Codex 开发文档
-> 输入门禁
-> 规划门禁
-> 治理配置门禁
-> 分支启动包
-> 多线程执行
-> 执行证据门禁
-> integration / test
-> 失败回流
-> 合入门禁
-> main 拍板
-> closeout / 反哺候选
```

#### 4.2.1 输入门禁

| 检查项 | 结论 | 缺失 / 风险 | 处理建议 |
|---|---|---|---|
| 需求是否完整 |  |  |  |
| 验收标准是否清楚 |  |  |  |
| API contract 是否明确 |  |  |  |
| 数据库 / 数据字段是否明确 |  |  |  |
| 页面状态是否明确 |  |  |  |
| AI 输出格式是否明确 |  |  |  |
| 依赖关系是否明确 |  |  |  |
| 风险点是否说明 |  |  |  |

输入门禁不通过时，不硬拆分支；只能拆低风险分支，或生成待决策问题。

#### 4.2.2 分支矩阵

| 分支名 | 任务节点 | 版本归属 | 优先级 | 影响面 | 依赖 | 可并行 | 冲突预测 | 风险等级 | 是否进入当前版本 |
|---|---|---|---|---|---|---|---|---|---|
|  |  | v0.1 / v0.2 / later / out-of-scope | P0 / P1 / P2 / P3 / P4 |  |  |  |  |  |  |

优先级规则：

- P0：阻塞级，不做它其他分支无法推进。
- P1：主链路，当前版本核心功能。
- P2：增强级，影响体验但不阻塞主流程。
- P3：辅助级，文档、低风险样式、普通优化。
- P4：可延后，非当前版本必要内容。

版本范围规则：

- 当前版本目标强相关：`v0.1`。
- 增强体验但不影响主流程：`v0.2`。
- 缺少明确验收标准：`later`。
- 与本轮目标无关：`out-of-scope`。

#### 4.2.3 冲突预测与分支预算

| 分支 A | 分支 B | 冲突类型 | 风险 | 处理方式 |
|---|---|---|---|---|
|  |  | 文件 / API / DB / 状态 / UI / 权限 |  |  |

| 预算项 | 建议值 | 当前值 | 处理动作 |
|---|---:|---:|---|
| 建议分支数 |  |  |  |
| 最大分支数 |  |  |  |
| 最大并行数 |  |  |  |
| 最大高风险分支数 |  |  |  |
| 最大待人工拍板点 |  |  |  |

超出预算时，合并低风险分支、拆版本、降低并行度，或生成 scope creep 报告。

#### 4.2.4 分支治理卡

| 分支目标 | 允许修改范围 | 禁止修改范围 | 责任管家 | Action 合同 | Artifact 合同 | Harness / Gate | 审核门禁 | 效率检查 | 执行证据 | Bug 处理 | 失败回流 | Closeout | 用户拍板条件 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |  |  |  |  |  |  |  |

skill、harness、plugin、automation 只能由治理配置按需启用，不能作为每个分支的默认堆叠。

#### 4.2.5 分支启动包

分支启动包是 Codex 真正执行分支任务的最小任务指令。

| 字段 | 内容 |
|---|---|
| 分支名 |  |
| 任务目标 |  |
| 上下文摘要 |  |
| 允许修改范围 |  |
| 禁止修改范围 |  |
| 依赖分支 |  |
| 相关文件 |  |
| 执行步骤 |  |
| 检查命令 |  |
| 验收标准 |  |
| 失败处理方式 |  |
| 输出证据要求 |  |

#### 4.2.6 Ready / Done 标准

Ready 标准：

- 目标清楚。
- 范围清楚。
- 禁止范围清楚。
- 依赖清楚。
- 验收标准清楚。
- 测试方式清楚。
- 回滚方式清楚。
- 分支启动包已生成。

Done 标准：

- 代码或文档变更完成。
- 自检完成。
- 测试完成。
- 必要 harness 完成。
- 管家审核完成。
- 执行证据完整。
- 回滚方式明确。
- closeout 已生成。

#### 4.2.7 Contract Freeze

| 契约 | 状态 | 依赖分支 | 变更处理 |
|---|---|---|---|
| API contract | draft / frozen / changed / deprecated |  |  |
| 数据库 schema | draft / frozen / changed / deprecated |  |  |
| AI 输出格式 | draft / frozen / changed / deprecated |  |  |
| 页面状态结构 | draft / frozen / changed / deprecated |  |  |
| 权限规则 | draft / frozen / changed / deprecated |  |  |
| 配置项 | draft / frozen / changed / deprecated |  |  |

关键契约冻结后，依赖分支才允许正式并行。冻结后变更必须生成 contract change 记录，并触发受影响分支重新评估。

#### 4.2.8 变更控制

分支执行中发现超出允许范围的修改时，不允许直接改，必须生成 scope change request。

| 变更请求 | 原因 | 选项 | 处理结论 |
|---|---|---|---|
|  |  | 扩展本分支 / 新建分支 / 延后下版本 / 待决策 |  |

#### 4.2.9 决策日志

| 决策项 | 决策原因 | 影响范围 | 替代方案 | 最终选择 | 是否需要用户批准 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

#### 4.2.10 分支状态机

```text
planned
-> ready
-> running
-> self_checked
-> reviewed
-> gate_passed
-> integration_pending
-> integration_passed / integration_failed
-> fix_required / closed / blocked
```

状态流转必须受 Ready / Done、执行证据、管家审核和 integration 结果控制。

#### 4.2.11 执行证据门禁

| 分支 | 自检证据 | 测试证据 | Harness 结果 | 管家审核 | 风险确认 | 回滚说明 |
|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |

没有证据的分支不能进入 integration / test。

#### 4.2.12 Integration / Test 与失败回流

- 所有分支先合入 integration / test，不直接合 main。
- integration 失败时，必须定位责任分支，生成失败定位报告，回到对应 fix 分支修复。
- 高风险未解决问题不能自动接受。

#### 4.2.13 权限边界

系统不能自动：

- 合 main。
- push / PR。
- 删除或迁移重要数据。
- 修改 stable 规则。
- 新增长期 skill。
- 新增长期 harness。
- 外部分发材料。
- 接受高风险未解决问题。
- 扩大版本范围。
- candidate 升 stable。
- 写长期记忆。

#### 4.2.14 Closeout / 反哺候选

| 分支 | Bug / 问题 | 修复方式 | 是否架构问题 | 是否项目特例 | 反哺候选 | 用户拍板项 |
|---|---|---|---|---|---|---|
|  |  |  |  |  | 模板 / prompt / skill / harness / workflow / registry / 长期偏好 |  |

教师机制并入 bug closeout、分支 closeout、项目 closeout 和版本 / 模型更新复核，不作为每个分支的默认独立机制。

---

## 5. 允许修改范围

| 路径 / 模块 | 允许动作 | 原因 |
|---|---|---|
|  |  |  |

---

## 6. 禁止修改范围

| 路径 / 模块 | 禁止原因 | 例外条件 |
|---|---|---|
|  |  |  |

默认禁止：

- 未批准的项目范围扩展。
- 未批准的数据库 schema 变更。
- 未批准的模型供应商、高成本模型或跨境数据变更。
- 未批准的长期记忆、stable rule、skill、harness、registry 变更。
- 未批准的删除、归档、push、PR、生产发布。

---

## 7. 人工确认点

| 确认点 | 触发条件 | 未确认时处理 |
|---|---|---|
| 产品范围变化 | 新增、删除或改变核心能力 | 暂停并回问 |
| 数据库 / 数据迁移 | schema、迁移、清洗、删除 | 暂停并回问 |
| 外部数据 / API / MCP | 接入新外部能力 | 暂停并回问 |
| AI 模型 / 成本 | 换模型、升成本、跨境或高风险输出 | 暂停并回问 |
| 发布 / 分发 | push、PR、上线、外部包 | 暂停并回问 |
| 长期规则 | skill、harness、registry、memory、stable policy | 先出方案，获批后再做 |

---

## 8. 验证命令

按项目实际情况填写，不要求所有项目都有同一套命令。

```bash
# syntax / lint

# unit / integration / API

# regression

# harness check-only when this repository governance is involved
python3 harness/run_harness.py --base-dir . --project <project-slug> --mode advisory --check-only
```

如果检查不能运行，必须记录：

- 不能运行的原因：
- 替代检查：
- 剩余风险：

---

## 9. 回滚方案

| 场景 | 回滚方式 | 验证方式 |
|---|---|---|
| 单文件改坏 | 只回滚对应文件 | 重跑相关检查 |
| 功能回退 | 关闭本轮入口或恢复旧逻辑 | 核心路径验证 |
| 数据风险 | 停止迁移或恢复备份方案 | 数据一致性检查 |
| 提交后撤销 | 优先 `git revert <commit>` | 重跑回归 |

不得使用 destructive reset 处理未经用户明确批准的工作区。

---

## 10. 待决策项

| 决策 | 选项 | 不同结果 | 建议 |
|---|---|---|---|
|  |  |  |  |

---

## 11. 审核结论

| 检查项 | 结论 | 备注 |
|---|---|---|
| 输入是否足够 |  |  |
| 任务包是否可执行 |  |  |
| 写入边界是否清楚 |  |  |
| 验证命令是否可运行 |  |  |
| 人工确认点是否完整 |  |  |
| 是否存在任务漂移 |  |  |
| 是否需要完整内部模式 |  | 默认否，除非触发 0.2 |

最终建议：

-

---

## 12. 完整内部治理附录

本附录仅用于 full internal / agentic delivery / 多阶段开发治理 / Skill-MCP-Harness 改造。普通开发文档不要默认展开本附录。

### 12.1 启用条件

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 用户明确要求完整 agentic delivery |  |  |
| 用户明确要求分期 Codex 计划 |  |  |
| 项目已批准能力启用 / MCP / harness / registry 范围 |  |  |
| 当前任务是治理架构或工具层修复 |  |  |

未满足启用条件时，停止展开附录，回到轻量模式。

### 12.2 能力启用检查

| 检查项 | 结论 | 处理动作 | 是否需人工确认 |
|---|---|---|---|
| 现有 skill 是否足够 |  |  |  |
| 是否真的需要新 skill |  |  | 是 |
| 是否真的需要 MCP 接入 |  |  | 是 |
| 是否真的需要 harness 改造 |  |  | 是 |
| 是否涉及 registry / workflow |  |  | 是 |
| 是否涉及外部 API / 模型 |  |  | 是 |

### 12.3 内部治理分工

仅在完整模式下填写。

| 角色 / 部门 | 职责 | 输出物 | 禁止事项 |
|---|---|---|---|
| 总控 | 主线识别、拆解、门禁、协调 | 任务包、状态、风险 | 不替代用户高风险拍板 |
| 产品 | 范围、流程、原型、验收 | 差异清单 | 不混入开发机制 |
| 架构 / 开发 | 技术方案、实现、验证 | 代码、测试、报告 | 不越过写入边界 |
| 风控 / 质量 | 合规、隐私、回归、审计 | 检查结果 | 不用主观感觉替代检查 |
| 学习 / 复盘 | 反馈分类、候选提案 | 学习提案 | 不自动写长期记忆 |

### 12.4 多阶段交付

只有用户明确要求分期计划时填写。

| 阶段 | 目标 | 交付物 | 用户侧效果 | 验收标准 |
|---|---|---|---|---|
| 一期 |  |  |  |  |
| 二期 |  |  |  |  |
| 三期 |  |  |  |  |
| 最终 |  |  |  |  |

### 12.5 完整模式检查

```bash
python3 harness/run_harness.py --base-dir . --project <project-slug> --mode advisory --check-only
python3 harness/run_harness.py --base-dir . --project <project-slug> --mode advisory --check-only --audit
python3 harness/run_harness.py --base-dir . --project <project-slug> --mode advisory --check-only --efficiency
```

如果完整模式涉及 skill、harness、registry、workflow、长期记忆或外部分发，必须先有用户批准的具体方案。
